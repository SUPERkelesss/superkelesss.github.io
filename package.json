{
  "name": "mkdocs",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "dev": "mkdocs serve",
    "build": "npm install pngquant && pip install -r requirements.txt && python3 -m mkdocs build -d public"
  }
}