{
  "name": "mkdocs",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "dev": "mkdocs serve",
    "build": "apt-get update && apt-get install -y libcairo2-dev libfreetype6-dev libffi-dev libjpeg-dev libpng-dev libz-dev && npm install pngquant && python3 -m mkdocs build -d public"
  }
}