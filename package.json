{
  "name": "mkdocs",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "dev": "mkdocs serve",
    "build": "dnf install -y cairo-devel freetype-devel libffi-devel libjpeg-devel libpng-devel zlib-devel && npm install pngquant && python3 -m mkdocs build -d public"
  }
}